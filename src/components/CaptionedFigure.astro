---
/**
 * ONLY meant for standalone figures (embeds, carousels, full-width screenshots).
 */
interface Props {
  caption: string;
  class?: string;
  spaced?: boolean;
}

const { caption, class: className, spaced = true } = Astro.props;
---

<figure class:list={[{ "mb-6": spaced }, className]}>
  <slot />
  <figcaption class="mt-4 text-end text-sm italic text-zinc-400">{caption}</figcaption>
</figure>
